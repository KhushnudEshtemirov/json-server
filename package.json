{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/server.js",
    "server": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-serve": "^0.1.0",
    "json-server": "^0.17.3"
  }
}
